const express = require('express');
const router = express.Router;
const productController = require('../controllers/product-controller');

// router.get('/', userControllers.getHomePage);

// product view
// router.get('/view-products', );

// sigle product view
// router.get('/view-sigleProduct/:id', productControllers.getProduct);

// router.get('/orders', userControllers.showOrders); 

// router.get('/cart', userControllers.showCart);

// router.get('/wishlist', userControllers.showWishlist);

module.exports = router;
